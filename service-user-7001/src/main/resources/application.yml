server:
  port: ${port:7001}
  tomcat:
    uri-encoding: utf-8
    max-swallow-size: -1
spring:
  application:
    name: user-service
#  profiles:
#    active: dev
logging:
  level:
    root: info
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      id-type: assign_id